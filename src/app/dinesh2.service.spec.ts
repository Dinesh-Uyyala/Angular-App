/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { Dinesh2Service } from './dinesh2.service';

describe('Service: Dinesh2', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [Dinesh2Service]
    });
  });

  it('should ...', inject([Dinesh2Service], (service: Dinesh2Service) => {
    expect(service).toBeTruthy();
  }));
});
